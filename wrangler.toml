name = "ballern-rooms"
main = "worker/index.ts"
compatibility_date = "2026-08-01"

# The room is the object, addressed by its code — see `idFromName` in worker/index.ts.
[[durable_objects.bindings]]
name = "ROOMS"
class_name = "Room"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["Room"]
